# Development environment

This directory will be the deployable Terraform root for the first Azure environment. It will configure providers, declare environment inputs, call reusable modules, and expose operational outputs.

Real `.tfvars` files are ignored. Only a sanitized `terraform.tfvars.example` will be committed.

No Terraform configuration has been added yet.
