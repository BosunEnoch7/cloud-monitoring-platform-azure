# Compute module

This module will own the Ubuntu virtual machine and directly related resources, including its network interface, public IP, OS disk configuration, identity, and bootstrap input.

The module boundary keeps compute lifecycle concerns separate from shared network policy. No Terraform resources have been added yet.
