# Network module

This module will own the environment's virtual network, subnet, network security group, and security rules. Its interface will use generic concepts such as address spaces and allowed administrative source ranges while its implementation uses Azure resources.

No Terraform resources have been added yet.
