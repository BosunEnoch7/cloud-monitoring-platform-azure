# Alertmanager

This area will contain a safe Alertmanager configuration template and documentation for email routing, grouping, inhibition, silencing, and secret injection.

SMTP credentials will not be committed. A deploy-time mechanism will render or supply the protected runtime configuration.
